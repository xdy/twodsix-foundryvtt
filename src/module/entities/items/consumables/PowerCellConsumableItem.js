import { BaseConsumableItem } from './BaseConsumableItem.js';

/** Consumable subclass for power_cell subtype (uses "Reload" label). */
export class PowerCellConsumableItem extends BaseConsumableItem {}
