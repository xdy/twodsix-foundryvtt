import { BaseConsumableItem } from './BaseConsumableItem.js';

/** Consumable subclass for magazine subtype (uses "Reload" label). */
export class MagazineConsumableItem extends BaseConsumableItem {}
